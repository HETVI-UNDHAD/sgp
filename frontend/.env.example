# In React, prefix env vars with REACT_APP_
# If your API is deployed at https://api.example.com set:
REACT_APP_API_URL=https://api.example.com
# Leave empty to use same origin (useful when serving frontend and backend together)
REACT_APP_API_URL=
